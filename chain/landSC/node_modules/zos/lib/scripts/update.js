'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _lodash = require('lodash');

var _lodash2 = _interopRequireDefault(_lodash);

var _stdout = require('../utils/stdout');

var _stdout2 = _interopRequireDefault(_stdout);

var _ControllerFor = require('../models/network/ControllerFor');

var _ControllerFor2 = _interopRequireDefault(_ControllerFor);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

exports.default = async function update({ contractAlias, proxyAddress, initMethod, initArgs, all, network, force = false, txParams = {}, networkFile = undefined }) {
  if (!contractAlias && !all) {
    throw Error('The contract name to update must be provided, or explicit upgrading all proxies in the application.');
  }

  const controller = new _ControllerFor2.default(network, txParams, networkFile);

  try {
    await controller.checkLocalContractsDeployed(!force);
    const proxies = await controller.upgradeProxies(contractAlias, proxyAddress, initMethod, initArgs);
    (0, _lodash2.default)(proxies).values().forEach(proxies => proxies.forEach(proxy => (0, _stdout2.default)(proxy.address)));
  } finally {
    controller.writeNetworkPackage();
  }
};