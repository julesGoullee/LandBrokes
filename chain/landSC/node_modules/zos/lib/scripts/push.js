"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _stdout = require("../utils/stdout");

var _stdout2 = _interopRequireDefault(_stdout);

var _ControllerFor = require("../models/network/ControllerFor");

var _ControllerFor2 = _interopRequireDefault(_ControllerFor);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

exports.default = async function push({ network, deployStdlib, reupload = false, txParams = {}, networkFile = undefined }) {
  const controller = (0, _ControllerFor2.default)(network, txParams, networkFile);

  try {
    if (deployStdlib && !controller.isLib) await controller.deployStdlib();
    await controller.push(reupload);
    (0, _stdout2.default)(controller.isLib ? controller.packageAddress : controller.appAddress);
  } finally {
    controller.writeNetworkPackage();
  }
};